---
description: "Judge ICP fit for an enriched account, grounded on your corpus, then score it deterministically."
argument-hint: "[account slug]"
---

# Classify (shorthand)

Corpus-grounded ICP classification + deterministic scoring for an enriched account.

## Invocation

`/gtm-icp:classify [slug]`

## Behavior

Read `skills/classify/SKILL.md` and execute its workflow with the arguments below as the skill input. This file is a thin delegator — do not reimplement skill logic here.

$ARGUMENTS
